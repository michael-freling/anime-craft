# Changelog

Written by [the release workflow](.github/workflows/release.yml) from the pull
requests merged since the previous tag — the same list that appears on the
[releases page](https://github.com/michael-freling/anime-craft/releases), so the
two cannot drift apart. Edit `VERSION` on `main` to cut a release; see
[docs/releasing.md](docs/releasing.md).

<!-- Releases are added below this line, newest first. -->
